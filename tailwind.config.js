/** @type {import('tailwindcss').Config} */
export const content = [
  "./src/**/*.{js,jsx,ts,tsx}", // includes all JS, JSX, TS, and TSX files in src directory
];
export const theme = {
  extend: {}, // Customize or extend the default theme here if needed
};
export const plugins = [];
